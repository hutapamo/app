{
  "name": "overtimer",
  "version": "0.1.0",
  "description": "The timer that keeps counting when you go over",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
